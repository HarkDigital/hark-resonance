/**
 * "Turn your phone upright" gate for short landscape phones.
 * STUB — the UI build replaces the internals. Keep the exports.
 */
export function mountRotateGate() {}
export function unmountRotateGate() {}
